import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1A73E8);
  static const background = Color(0xFFF6F9FE);
  static const accent = Color(0xFF00C853);
}

class AppText {
  static const title = TextStyle(fontSize: 22, fontWeight: FontWeight.w600);
  static const body = TextStyle(fontSize: 16);
  static const caption = TextStyle(fontSize: 12, color: Colors.grey);
}

class AppConfig {
  static const supportedLanguages = ['si', 'ta', 'en']; // Sinhala, Tamil, English
}
